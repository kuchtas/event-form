const FormTitle = ({ title }) => {
  return (
    <div
      style={{
        textAlign: "center",
        fontSize: "7vw",
        fontWeight: "900",
        height: "10%",
        color: "#4571a8",
      }}
    >
      {title}
    </div>
  );
};

export default FormTitle;
