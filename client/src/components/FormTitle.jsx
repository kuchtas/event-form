const FormTitle = ({ title }) => {
  return (
    <div
      style={{
        textAlign: "center",
        fontSize: "50px",
        fontWeight: "900",
        height: "10%",
      }}
    >
      {title}
    </div>
  );
};

export default FormTitle;
